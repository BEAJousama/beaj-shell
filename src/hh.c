				// tmp = ft_strdup(i -> key);
				// free(i -> key);
				// i -> key = ft_strdup(j -> key); 
				// free(j -> key);
				// j -> key = ft_strdup(tmp);
				// free(tmp);

				// tmpv = ft_strdup(i -> value);
				// free(i -> value);
				// i -> value = ft_strdup(j -> value); 
				// free(j -> value);
				// j -> value = ft_strdup(tmpv);
				// free(tmpv);